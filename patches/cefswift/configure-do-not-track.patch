diff --git a/Sources/CefKit/BrowserClient.swift b/Sources/CefKit/BrowserClient.swift
index 44a10e0..5665dde 100644
--- a/Sources/CefKit/BrowserClient.swift
+++ b/Sources/CefKit/BrowserClient.swift
@@ -2,6 +2,32 @@ import AppKit
 import CCef
 import Foundation
 
+/// Process-wide request policy used from CEF's IO thread.
+///
+/// CEF's native `enable_do_not_track` preference is not safe to mutate in the
+/// Chrome runtime used by Astra. Applying the header at the request boundary
+/// preserves the setting without touching Chromium's preference store.
+final class CefDoNotTrackPolicy: @unchecked Sendable {
+    static let shared = CefDoNotTrackPolicy()
+
+    private let lock = NSLock()
+    private var enabled = false
+
+    private init() {}
+
+    func setEnabled(_ enabled: Bool) {
+        lock.lock()
+        self.enabled = enabled
+        lock.unlock()
+    }
+
+    func isEnabled() -> Bool {
+        lock.lock()
+        defer { lock.unlock() }
+        return enabled
+    }
+}
+
 /// Swift owner behind the `cef_client_t` + handler structs for one browser.
 ///
 /// CEF invokes the struct callbacks on its UI thread, which is the main
@@ -30,6 +56,7 @@ final class BrowserClient {
     nonisolated(unsafe) var contextMenuPointer: UnsafeMutablePointer<cef_context_menu_handler_t>?
     nonisolated(unsafe) var permissionPointer: UnsafeMutablePointer<cef_permission_handler_t>?
     nonisolated(unsafe) var requestPointer: UnsafeMutablePointer<cef_request_handler_t>?
+    nonisolated(unsafe) private var resourceRequestPointer: UnsafeMutablePointer<cef_resource_request_handler_t>?
     nonisolated(unsafe) var keyboardPointer: UnsafeMutablePointer<cef_keyboard_handler_t>?
     nonisolated(unsafe) var focusPointer: UnsafeMutablePointer<cef_focus_handler_t>?
     nonisolated(unsafe) var renderPointer: UnsafeMutablePointer<cef_render_handler_t>?
@@ -655,6 +682,30 @@ extension BrowserClient {
     // MARK: Navigation / requests
 
     private func makeRequestHandler() {
+        let resourceHandler = cefAllocate(cef_resource_request_handler_t.self, owner: self)
+        resourceHandler.pointee.on_before_resource_load = {
+            _, browser, frame, request, callback in
+            cefRelease(browser.map(UnsafeMutableRawPointer.init))
+            cefRelease(frame.map(UnsafeMutableRawPointer.init))
+            cefRelease(callback.map(UnsafeMutableRawPointer.init))
+            guard let request else { return RV_CONTINUE }
+            defer { cefRelease(UnsafeMutableRawPointer(request)) }
+
+            guard CefDoNotTrackPolicy.shared.isEnabled() else { return RV_CONTINUE }
+            let urlString = CefStringUtil.takingUserFree(request.pointee.get_url?(request)) ?? ""
+            guard let scheme = URL(string: urlString)?.scheme?.lowercased(),
+                  scheme == "http" || scheme == "https" else {
+                return RV_CONTINUE
+            }
+            CefStringUtil.withCefString("DNT") { name in
+                CefStringUtil.withCefString("1") { value in
+                    request.pointee.set_header_by_name?(request, name, value, 1)
+                }
+            }
+            return RV_CONTINUE
+        }
+        resourceRequestPointer = resourceHandler
+
         let handler = cefAllocate(cef_request_handler_t.self, owner: self)
         handler.pointee.on_before_browse = { handlerSelf, browser, frame, request, userGesture, isRedirect in
             cefRelease(browser.map(UnsafeMutableRawPointer.init))
@@ -737,9 +788,18 @@ extension BrowserClient {
                 cefBrowser.delegate?.browser(cefBrowser, renderProcessDidTerminate: reason, errorCode: code)
             }
         }
-        // get_resource_request_handler left unset (returns NULL by default) —
-        // the custom-scheme path already covers app-served content; per-response
-        // interception is intentionally out of scope.
+        handler.pointee.get_resource_request_handler = {
+            handlerSelf, browser, frame, request, _, _, _, _ in
+            cefRelease(browser.map(UnsafeMutableRawPointer.init))
+            cefRelease(frame.map(UnsafeMutableRawPointer.init))
+            cefRelease(request.map(UnsafeMutableRawPointer.init))
+            guard let client = BrowserClient.owner(handlerSelf.map(UnsafeMutableRawPointer.init)),
+                  let resourceHandler = client.resourceRequestPointer else {
+                return nil
+            }
+            cefAddRef(UnsafeMutableRawPointer(resourceHandler))
+            return resourceHandler
+        }
         requestPointer = handler
     }
 
diff --git a/Sources/CefKit/CefConfiguration.swift b/Sources/CefKit/CefConfiguration.swift
index 72fbe11..d9b5f03 100644
--- a/Sources/CefKit/CefConfiguration.swift
+++ b/Sources/CefKit/CefConfiguration.swift
@@ -100,6 +100,10 @@ public struct CefConfiguration: Sendable {
     /// Persist session cookies across runs.
     public var persistSessionCookies: Bool = false
 
+    /// Sends the standard `DNT: 1` request header and exposes the matching
+    /// JavaScript browser preference when enabled.
+    public var doNotTrackEnabled: Bool = false
+
     /// Default runtime style for browsers created without an explicit style.
     public var defaultRuntimeStyle: CefRuntimeStyle = .default
 
diff --git a/Sources/CefKit/CefProfile.swift b/Sources/CefKit/CefProfile.swift
index fead94b..1727375 100644
--- a/Sources/CefKit/CefProfile.swift
+++ b/Sources/CefKit/CefProfile.swift
@@ -109,6 +109,13 @@ public final class CefProfile {
         }
     }
 
+    /// Updates the process-wide request-layer Do Not Track policy.
+    @discardableResult
+    public func setDoNotTrackEnabled(_ enabled: Bool) -> Bool {
+        CefDoNotTrackPolicy.shared.setEnabled(enabled)
+        return true
+    }
+
     private func createContext() -> UnsafeMutablePointer<cef_request_context_t>? {
         var settings = cef_request_context_settings_t()
         settings.size = MemoryLayout<cef_request_context_settings_t>.stride
@@ -121,10 +128,15 @@ public final class CefProfile {
                 settings.persist_session_cookies = 1
             }
         }
+        CefStringUtil.set(
+            CefRuntime.shared.configuration?.acceptLanguageList,
+            into: &settings.accept_language_list
+        )
         // Empty cache_path => incognito/in-memory (the CEF default).
-        defer { ccef_string_clear(&settings.cache_path) }
-        // No request-context handler needed: cookies/storage isolation is
-        // driven entirely by the distinct cache_path.
+        defer {
+            ccef_string_clear(&settings.cache_path)
+            ccef_string_clear(&settings.accept_language_list)
+        }
         return cef_request_context_create_context(&settings, nil)
     }
 }
diff --git a/Sources/CefKit/CefRuntime.swift b/Sources/CefKit/CefRuntime.swift
index a8ee87c..dc41007 100644
--- a/Sources/CefKit/CefRuntime.swift
+++ b/Sources/CefKit/CefRuntime.swift
@@ -138,6 +138,7 @@ public final class CefRuntime {
     public func initialize(configuration: CefConfiguration = .default) throws(CefError) {
         precondition(Thread.isMainThread, "CefRuntime.initialize must run on the main thread.")
         guard !isInitialized else { throw CefError.alreadyInitialized }
+        CefDoNotTrackPolicy.shared.setEnabled(configuration.doNotTrackEnabled)
 
         // CEFApplication must exist before cef_initialize.
         if NSApp == nil {
@@ -212,6 +213,15 @@ public final class CefRuntime {
         registerSchemeHandler(scheme: CefBridge.schemeName, handler: CefBridgeSchemeHandler(bridge: bridge))
     }
 
+    /// Updates the request-layer Do Not Track policy for all browser profiles.
+    @discardableResult
+    public func setDoNotTrackEnabled(_ enabled: Bool) -> Bool {
+        precondition(Thread.isMainThread, "CEF request policy must be changed on the main thread.")
+        CefDoNotTrackPolicy.shared.setEnabled(enabled)
+        configuration?.doNotTrackEnabled = enabled
+        return true
+    }
+
     // MARK: Custom scheme handlers
 
     /// Registers `handler` as the content source for `scheme` (optionally
